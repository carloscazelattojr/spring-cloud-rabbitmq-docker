spring:
  application:
    name: mscartoes
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://mseurekauser:senhaeureka@localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance.instace-id:${random.value}}

mq:
  queues:
    emissao-cartoes: emissao-cartoes

